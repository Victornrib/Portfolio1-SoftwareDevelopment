package model;

public class Circle extends Shape{

    Point center;
    Integer radius;

    Circle(Point center, Integer radius) {
        this.center = center;
        this.radius = radius;
    }
}
